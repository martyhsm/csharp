namespace Marty
{
    /// <summary>
    /// Defines constants for Marty
    /// </summary>
    internal class MartyConstants
    {
        // Stores name of Top state
        public const string TopStateName = "Top";

        /// <summary>
        /// Stores regex for a state's name
        /// </summary>
        internal const string StateNameRegex = @"[a-zA-Z][a-zA-Z0-9_-]*";
    }
}
